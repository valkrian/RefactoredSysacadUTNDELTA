# .env.example
# Archivo de ejemplo para variables de entorno
# Copia este archivo a .env y ajusta los valores según necesites
# ⚠️ El archivo .env NO debe subirse a Git (ya está en .gitignore)

# PostgreSQL Configuration
# Estas variables se usan en docker-compose.yml
POSTGRES_DB=autogestion_db
POSTGRES_USER=autogestion_user
POSTGRES_PASSWORD=autogestion_password

# PostgreSQL Connection (para uso en .NET)
# Host y puerto para conectarse desde la aplicación
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Connection String para .NET (opcional)
# Descomenta y ajusta si necesitas una connection string completa
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=autogestion_db;Username=autogestion_user;Password=autogestion_password
